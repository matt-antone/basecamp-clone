node_modules/
.next/
.next_bak_*/
.netlify/
.env
.env.*
!.env.example
.DS_Store
coverage/
tmp.json
*.tsbuildinfo
.temp 
node-*
orchestration/*
# Root-only: do not use bare `thumbnail-*` — it ignores lib/thumbnail-*.ts and breaks builds.
/thumbnail-*
certificates

# Cursor debug session logs (do not commit)
.cursor/debug*.log
.vercel
